## **ЛАБОРАТОРНА РОБОТА №4**

**Тема:** Реалізація прототипу чат-бота на основі дерева рішень
**Виконав:** студент 5 курсу, групи КН-425 — **Сергій Чалий**
**Перевірила:** викладач — **Оксана Іващенко**
**Харків — 2025**

---

### **Мета роботи**

Реалізувати rule-based чат-бот на основі дерева рішень для інтернет-магазину сантехніки за допомогою Python і бібліотеки `aiogram`. Навчитись створювати базову структуру бота, обробляти стани користувача та реалізовувати просту бізнес-логіку без використання моделей машинного навчання.

---

### **Вибір технології (чому саме aiogram)**

Для реалізації прототипу було обрано бібліотеку **`aiogram`**, оскільки:

* вона побудована на **асинхронній архітектурі**, що дозволяє ефективно обробляти багато користувачів одночасно;
* має **зручну систему станів (FSM)** для побудови діалогів;
* **легко налаштовується** — достатньо кількох рядків коду для запуску;
* має **велику спільноту та документацію**;
* дозволяє реалізувати **rule-based логіку** без складних фреймворків типу Rasa чи Dialogflow.

Таким чином, `aiogram` забезпечує баланс між простотою реалізації та гнучкістю структури.

---

### **Логіка реалізації згідно з попереднім деревом рішень**

Чат-бот реалізує класичну логіку rule-based системи, де всі переходи наперед визначені в дереві рішень.
Основні стани діалогу:

1. **Привітання** — бот пояснює можливості та показує головне меню.
2. **Каталог** — користувач обирає категорію товару (унітази, раковини, ванни, змішувачі).
3. **Уточнення** — бот запитує деталі (бренд, ціна тощо).
4. **Додавання до кошика** — користувач підтверджує покупку.
5. **Оформлення замовлення** — бот запитує ім’я, телефон, адресу.
6. **Оплата** — користувач обирає спосіб оплати.
7. **Підтвердження** — бот надсилає підсумок замовлення і завершує діалог.
8. **Зв’язок з оператором** — опція для нестандартних запитів.

Усі переходи між станами здійснюються за допомогою FSM (`Finite State Machine`), що забезпечує контроль діалогу.

---

### **Ключові етапи реалізації**

1. **Ініціалізація бота:**

   * створено бота з токеном;
   * підключено `Dispatcher` з `MemoryStorage` для FSM.

2. **Визначення станів:**

   * створено клас `OrderStates` із кроками: `choosing_category`, `choosing_product`, `confirm_add`, `checkout_name`, `checkout_phone`, `checkout_address`, `choosing_payment`.

3. **Побудова меню та клавіатур:**

   * головне меню (каталог, доставка, кошик, зв’язок з оператором);
   * динамічні клавіатури для вибору категорій, товарів, оплати.

4. **Обробники (handler-и):**

   * `/start` — привітання і показ головного меню;
   * вибір категорії → відображення товарів;
   * підтвердження покупки → оформлення замовлення;
   * введення контактних даних;
   * вибір способу оплати → підтвердження.

5. **Збереження даних:**

   * у словнику `CARTS` зберігається кошик користувача (у пам’яті);
   * після оформлення замовлення — очищення кошика.

6. **Обробка винятків:**

   * якщо користувач вводить некоректні дані або виходить із сценарію — бот повертає підказки з меню.

---

### **Короткий приклад коду**

```python
from aiogram import Bot, Dispatcher, executor, types
from aiogram.contrib.fsm_storage.memory import MemoryStorage
from aiogram.dispatcher import FSMContext
from aiogram.dispatcher.filters.state import State, StatesGroup

bot = Bot(token="YOUR_BOT_TOKEN")
dp = Dispatcher(bot, storage=MemoryStorage())

class OrderStates(StatesGroup):
    choosing_category = State()
    choosing_product = State()

@dp.message_handler(commands=["start"])
async def start_cmd(message: types.Message):
    kb = types.ReplyKeyboardMarkup(resize_keyboard=True)
    kb.add("📚 Каталог", "ℹ️ Доставка/Оплата")
    await message.answer("Вітаю! Оберіть дію:", reply_markup=kb)

@dp.message_handler(lambda msg: msg.text == "📚 Каталог")
async def show_categories(message: types.Message):
    kb = types.ReplyKeyboardMarkup(resize_keyboard=True)
    kb.add("Унітази", "Раковини", "Ванни", "Змішувачі")
    await OrderStates.choosing_category.set()
    await message.answer("Оберіть категорію товарів:", reply_markup=kb)
```

*(повний код реалізації наведено у лабораторній роботі та може бути запущений як окремий файл `bot.py`)*

---

### **Результати тестування**

Після запуску бота було перевірено такі сценарії:

| № | Сценарій                      | Очікувана поведінка           | Результат |
| - | ----------------------------- | ----------------------------- | --------- |
| 1 | Команда `/start`              | Привітання та меню            | ✅ Успішно |
| 2 | Вибір категорії «Ванни»       | Показано список товарів       | ✅ Успішно |
| 3 | Додавання товару до кошика    | Підтвердження «додано»        | ✅ Успішно |
| 4 | Оформлення замовлення         | Запитано ПІБ, телефон, адресу | ✅ Успішно |
| 5 | Введення невалідного телефону | Повідомлення про помилку      | ✅ Успішно |
| 6 | Вибір способу оплати          | Підтвердження замовлення      | ✅ Успішно |
| 7 | Запит поза сценарієм          | Бот пропонує меню             | ✅ Успішно |

Бот стабільно обробляє основні стани, повертає користувача до меню у разі некоректних дій і не “зависає” в середині діалогу.

---

### **Висновки**

У ході лабораторної роботи було розроблено rule-based чат-бот інтернет-магазину сантехніки на базі бібліотеки `aiogram`.
Було реалізовано сценарії пошуку, додавання товару, оформлення та оплати замовлення, а також базову обробку виняткових ситуацій.
Отримано практичні навички роботи з FSM, клавіатурами та handler-логікою.
Бот готовий до подальшого розширення — підключення бази даних, інтеграції платіжних систем та аналітики.

---

### **Контрольні питання (з відповідями)**

1. **Яка основна мета rule-based чат-бота?**
   Автоматизація типових сценаріїв спілкування без залучення оператора.

2. **Яку роль відіграє FSM у `aiogram`?**
   Керує станами користувача і дозволяє контролювати логіку діалогу.

3. **Чому `aiogram` підходить для створення чат-ботів?**
   Має простий синтаксис, підтримку асинхронності та потужну FSM-систему.

4. **Що таке handler у `aiogram`?**
   Це функція, що реагує на конкретне повідомлення або дію користувача.

5. **Як зберігаються дані користувача у прототипі?**
   У тимчасовому словнику `CARTS` (у пам’яті програми).

6. **Що робить бот при неправильному вводі даних?**
   Видає підказку або повертає до меню.

7. **Як протестувати rule-based бота?**
   Пройти всі сценарії вручну в Telegram, перевіривши правильність переходів.

8. **Які обмеження такого підходу?**
   Відсутність гнучкості — бот реагує лише на заздалегідь задані фрази.

9. **Як розширити функціонал?**
   Додати базу даних, систему авторизації, аналітику, інтеграції з API.

10. **Що було головним результатом лабораторної?**
    Повноцінний прототип чат-бота з реалізованими сценаріями діалогу.
